# 2026.08 갱신 내역

포트폴리오 사이트(`info.yeopeva.me`)의 확정 이력을 기준으로 이력서 6종과 경력기술서를 맞췄습니다.

## 휴대폰 번호

이 저장소는 공개(public)이며 갱신 전 상태의 `.tex` 6개·`.pdf` 6개와 경력기술서에
`010-8652-9464` 가 들어 있었습니다. **공개해도 무방하다고 확인받아** 별도 조치는 하지
않았으나, 이번 갱신에서는 머리말을 아래처럼 정리해 번호 없이 빌드했습니다.

```
이창엽 (Changyeob Lee)        Github / LinkedIn
https://info.yeopeva.me/      Email: lcy606@naver.com
noperfectsecurity.tistory.com KST / GMT+9
```

번호를 다시 넣으려면 각 `.tex` 머리말 3행 끝에 `& Phone: 010-8652-9464 $|$ KST / GMT+9`
형태로 복원하면 됩니다 (현재는 `& KST / GMT+9`).

## 반영한 내용

| 항목 | 이전 | 갱신 |
|---|---|---|
| i-Keeper 기간 | 2023.09 – 2024.02 | **2023.09 – 2024.07** · CERT팀(–2024.02) / DEV팀(2024.03–) 분리 |
| 부산소프트웨어마이스터고 | 이력서에 **없음** / 경력기술서는 산학겸임교사만 (2026.03–07) | **2023.04 – 현재** · 전공동아리 멘토(2023–2026학년도) + 산학겸임교사(2026) |
| 디지털 범인을 찾아라 | 제8–10회 (3회) | **제8–11회 (4회 연속)** · 2025.12.16 추가 |
| 자격 | 5건 | **7건** — Exterro ACE / AccessData(2022.01.11–2024.01.11, 만료), 인터넷정보관리사 2급(2020.10) 추가 |
| AWS SAA | 기간만 | **만료** 표기 (2026.07.26 경과) |
| 발표 및 논문 | 4건 | **6건** — 카드 해킹의 문제점과 대비방안(2018), RFID Skimming 해킹 기법에 관한 고찰(2017) 추가 |
| 주요 수상 순서 | 금융보안아카데미부터 | **최신순 재정렬** — 디범찾(2025.12.16)이 최상단 |
| 기술 및 관심 분야 | `Security / Engineering / Languages` 3줄, `DFIR, 침해사고대응` 중복 | 사이트와 동일한 **4개 영역** + 관심 분야 + 언어 |

### DFIR 표기 정리

`DFIR` = **D**igital **F**orensics / **I**ncident **R**esponse 이므로
`DFIR, 침해사고대응` · `DFIR / Incident Response` · `DFIR·침해대응 경진대회` 는 침해대응이 두 번 들어갑니다.

- 기술 항목: `디지털 포렌식 · 침해대응 (DFIR)` — 약어를 전체 표현 뒤 괄호로
- 경진대회: `디지털 포렌식 · 침해대응 경진대회`
- `DFIR 중심 CTF` 처럼 단독으로 쓰는 곳은 그대로 둡니다

## 회신 반영 (2026.08.19)

| 항목 | 결정 | 처리 |
|---|---|---|
| 대표 경험 순서 | 사이트 순서로 통일 | `HSPACE → SCH → 부산소마고 → i-Keeper → Anti-root → 공군` (진행 중 우선 → 종료일 내림차순) — 이력서 6종·경력기술서 모두 |
| 경력기술서 페이지 수 | 보조 설명 최대한 살릴 것 | **4페이지 → 3페이지.** 내용은 한 줄도 덜어내지 않았습니다 (`resumeItem` 59개 그대로) |
| 순천향대 학점 | 현재 점수 유지 | `3.55/4.5 (3-2 종료 기준)` 그대로 |
| 학회명 | 정보보호학회는 `동계학술대회`, 그 외는 `종합` | 한국데이터사이언스학회만 **`동계 종합학술대회`** (영문 `Winter General Conference`)로 수정. 한국정보보호학회 4건은 그대로 |

### 경력기술서를 3페이지로 줄인 방법

내용을 깎지 않고 **항목 사이 '단락 간격'(`parsep`)만 없앴습니다.**

```latex
\newcommand{\resumeItemListStart}{\begin{itemize}[topsep=2pt,parsep=0pt,partopsep=0pt]}
```

`resumeItem` 이 59개라 항목당 몇 pt 만 줄여도 한 페이지가 확보됩니다.
결과: 페이지별 하단 여백 40 / 45 / 42pt — 세 페이지가 고르게 찼습니다.

> **`itemsep` 은 건드리면 안 됩니다.** `\resumeItem` 정의 안에 이미 `\vspace{-2pt}` 가 있어
> `itemsep=0pt` 를 함께 주면 줄 간격이 음수가 되어 **글자가 겹칩니다.** 실제로 한 번 겹쳤고,
> 아래 검사기로 잡아 되돌렸습니다.

빌드 후에는 반드시 줄 충돌을 검사하세요. 좌우로 나란한 텍스트(`tabular*` 머리말 등)를
오탐하지 않도록 **세로·가로 겹침을 함께** 봅니다.

```bash
python3 - "이창엽_경력기술서.pdf" <<'EOF'
import pymupdf, sys
d=pymupdf.open(sys.argv[1]); bad=[]
for pi,p in enumerate(d,1):
    lines=[]
    for b in p.get_text("dict")["blocks"]:
        for l in b.get("lines",[]):
            x0,y0,x1,y1=l["bbox"]
            lines.append((y0,y1,x0,x1,"".join(s["text"] for s in l["spans"])[:30]))
    for i in range(len(lines)):
        for j in range(i+1,len(lines)):
            a,b_=lines[i],lines[j]
            if min(a[1],b_[1])-max(a[0],b_[0])>1.5 and min(a[3],b_[3])-max(a[2],b_[2])>2:
                bad.append((pi,a[4],b_[4]))
print(f'{d.page_count}페이지 / 충돌 {len(bad)}건')
for x in bad[:6]: print(' ', x)
EOF
```

## 빌드

`tectonic` 으로 빌드했습니다 (`brew install tectonic`).

```bash
cd resume/tex && for f in resume_ko resume_en resume_dfir_ko resume_dfir_en resume_cert_ko resume_cert_en; do
  tectonic -k -o . $f.tex
done && mv *.pdf ../pdf/

cd ../../work_experience && tectonic -k -o . 이창엽_경력기술서.tex
```

산출물: 이력서 6종 각 3페이지, 경력기술서 3페이지. 전부 줄 충돌 0건.
사이트의 `assets/resume/resume_dfir_{ko,en}.pdf` 도 이 빌드본으로 교체했습니다.

## 빌드 후 점검

```bash
python3 -c "
import pymupdf, glob, re
for f in sorted(glob.glob('resume/pdf/*.pdf'))+['work_experience/이창엽_경력기술서.pdf']:
    d=pymupdf.open(f); t=re.sub(r'\s+','',' '.join(p.get_text() for p in d))
    print(f'{f.split(\"/\")[-1]:<28} {d.page_count}p  번호{\"있음⚠️\" if \"8652\" in t else \"없음\"}')
"
```
